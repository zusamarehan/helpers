# helpers

## Bash

### Git URL Creator

Create a pull request URL from the current branch to a target branch.

**Usage:**
```bash
pr target-branch
```

The current branch will be used to create a PR URL to the target branch.